
# test logger
file(GLOB loggerlist ${CMAKE_CURRENT_SOURCE_DIR}/logger/*.cpp)
add_executable(test_logger ${loggerlist})
target_include_directories(test_logger PUBLIC ${CMAKE_SOURCE_DIR})
target_include_directories(test_logger PUBLIC ${CMAKE_SOURCE_DIR}/lib/doctest) # include doctest
target_link_libraries(test_logger PRIVATE liblogger)
add_test(NAME logger_test COMMAND test_logger)

# test queue
file(GLOB queuelist ${CMAKE_CURRENT_SOURCE_DIR}/queue/*.cpp)
add_executable(test_queue ${queuelist})
target_include_directories(test_queue PUBLIC ${CMAKE_SOURCE_DIR}/include)
target_include_directories(test_queue PUBLIC ${CMAKE_SOURCE_DIR}/lib/doctest) # include doctest
add_test(NAME queue_test COMMAND test_queue)